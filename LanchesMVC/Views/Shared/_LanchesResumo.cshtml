@model Lanche

<div class="col-sm-4 col-md-4 col-lg4">
    <img src="@Model.ImagemUrl" class="img-fluid" />
    <h3>Preço: @Model.Preco.ToString("c");</h3>
    <h4>@Model.Nome</h4>
    <span>@Model.DescricaoCurta</span>
</div>